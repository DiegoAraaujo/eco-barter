// Caminho do arquivo: src/context/TradeContext.jsx

// Define e exporta apenas o contexto em si (sem lógica).
// Mantém um único createContext para evitar instâncias duplicadas.

import React from "react";

export const TradeContext = React.createContext(null);
